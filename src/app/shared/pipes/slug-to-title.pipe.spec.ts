import { SlugToTitlePipe } from './slug-to-title.pipe';

describe('SlugToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SlugToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
